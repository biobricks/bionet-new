import {h} from 'preact';

module.exports = function(Component) {

  return class MapGrid extends Component {

    render() {
      return (
        <div class="MapGrid">
          MapGrid         
        </div>
      )
    }
  }
}
